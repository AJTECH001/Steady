# Steady — environment template (fill in; do not commit real values)

# Deployer
PRIVATE_KEY=

# RPC endpoints (trigger-only cross-chain: origin emits, reactive fires, destination executes)
ORIGIN_RPC_URL=
DESTINATION_RPC_URL=
REACTIVE_RPC_URL=

# Uniswap V4 (destination chain) — [VERIFY] real PoolManager per network before deploy
POOL_MANAGER=

# Verification
ETHERSCAN_API_KEY=
