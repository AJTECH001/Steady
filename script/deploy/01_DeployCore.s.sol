// SPDX-License-Identifier: MIT
pragma solidity ^0.8.26;

import {BaseScript} from "script/base/BaseScript.sol";

/// @notice Deploys Steady core storage contracts (registry + vault).
/// @dev Phase 2 scaffold — deploys nothing yet.
contract DeployCore is BaseScript {
    function run() public {
        // scaffold: deploy SteadyPlanRegistry + SteadyVault in Phase 2
    }
}
